# Component Rules

## Purpose
- Use these rules as guidelines when creating or modifying reusable UI components.
- These rules define component structure, behavior, and interaction patterns only.
- Visual styling (colors, typography, spacing tokens, shadows, etc.) is handled separately by the design system.


---



# Cards

## Card actions
- Secondary actions should be hidden by default unless prompted to be included.